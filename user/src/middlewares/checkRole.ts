
// import { Request, Response, NextFunction } from 'express';
export function isAdmin(roleName: string, ) {
    // check req.user.roleName
    // if (req.user.roleName === 'admin') {
    //     next();
    // } else {
    //     res.status(403).send('Unauthorized');
    // }
}